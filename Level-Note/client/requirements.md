## Packages
file-saver | For saving CSV files on the client side
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
lucide-react | Icon library

## Notes
App relies heavily on LocalStorage.
No backend database required for core functionality.
CSV export must include UTF-8 BOM.
UI must be optimized for mobile (iPhone) usage.
Dark mode is essential.
